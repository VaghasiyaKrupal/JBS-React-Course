// console.log("hello js",2+3);

const name = undefined;
console.log(typeof(undefined));

// javascript datatypes:
// 1. primitive

// key = nn ss bb u

// 1. null - typeof null returns "object" because of a historical bug in JavaScript’s implementation.
// 2. number 
// 3. string 
// 4. symbole → is a function (constructor), so typeof Symbol gives "function". (its the function that create a symbols)
// 5. boolean 
// 6. bigInt
// 7. undefine - typeof returns the same string as the value’s name. Because undefined is its own type in JavaScript

// 2. non-primitive